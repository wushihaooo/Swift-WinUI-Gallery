<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Padding="36,36,36,0">
    <Grid x:Name="rootPanel">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!-- Header -->
        <Grid x:Name="headerGrid"/>
        <!-- Body -->
        <ScrollViewer Padding="36,0,36,0" Grid.Row="1">
            <StackPanel x:Name="exampleStackPanel" Spacing="8">
                <Grid x:Name="Example1" Height="Auto" HorizontalAlignment="Left">
                    <StackPanel>
                        <TextBlock Margin="0,0,0,15">
                            This is a basic GridView that has the full source code below.<LineBreak />
                            Other samples on this page display only the additional markup needed to customize the specific GridView.</TextBlock>
                        <GridView
                            x:Name="exampleGridView1"
                            IsItemClickEnabled="True"
                            SelectionMode="Single" />
                        <TextBlock x:Name="ClickOutput0" />
                    </StackPanel>
                </Grid>
                <Grid x:Name="Example2" Height="Auto" HorizontalAlignment="Left">
                    <StackPanel>
                        <TextBlock Margin="0,0,0,15" TextWrapping="Wrap">
                            Use the options on the right to control different layout customizations to the GridView below.
                        </TextBlock>
                        <GridView x:Name="StyledGrid">
                        </GridView>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>