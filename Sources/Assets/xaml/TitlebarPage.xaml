<Page
    x:Class="SwiftGallery.TitlebarPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <!-- Header -->
            <RowDefinition Height="Auto" />
            <!-- 示例区域 -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 顶部 header 占位，由 TitleBarPage.swift 里的 setupHeader() 填充 -->
        <Grid x:Name="headerGrid"
              Grid.Row="0" />

        <!-- 示例区域：外层 ScrollViewer + StackPanel -->
        <ScrollViewer Grid.Row="1">
            <StackPanel x:Name="exampleStackPanel"
                        Orientation="Vertical"
                        Spacing="16"
                        Padding="36,24,36,36">

                <!-- Example1：TitleBar configuration 示例占位 -->
                <Grid x:Name="Example1" />

                <!-- 如果以后要加第二个示例，可以在这里再放 Example2 的占位 -->
                <!-- <Grid x:Name="Example2" /> -->

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
